<header>
  <nav>
    <ul id="menu">
      <% if user_signed_in? %>
        <li>
          <%= link_to t('.widgets'), widgets_path %>
        </li>
        <li>
          <%= link_to t('.account'), current_user.profile %>
        </li>
        <li>
          <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
        </li>
      <% end %>
    </ul>
  </nav>
  <% if user_signed_in? %>
    <p style="text-align: right;"><%= l Time.current %></p>
  <% end %>
</header>
<p id="notice"><%= notice %></p>
