<header>
  <nav>
    <ul id="menu">
      <% if user_signed_in? %>
        <li>
          <%= link_to 'Widgets', widgets_path %>
        </li>
        <li>
          <%= link_to 'My Account', current_user.profile %>
        </li>
        <li>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
        </li>
      <% end %>
    </ul>
  </nav>
  <% if user_signed_in? %>
    <p style="text-align: right;"><%= l Time.current %></p>
  <% end %>
</header>
<p id="notice"><%= notice %></p>
